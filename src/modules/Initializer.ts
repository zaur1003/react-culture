import { InitializerInterface } from "../types/types";

export const main = (initConfig: InitializerInterface): void => {
  // We need to make some checks
  // Use getCanonicalLocales and SupportedValuesOf
};

export const reconfigure = (initConfig: InitializerInterface): void => {
  // We need to make some checks
  // Use getCanonicalLocales and SupportedValuesOf
};
